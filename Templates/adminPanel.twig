{% extends 'base.twig' %}

{% block content %}
    <div id="admin-panel-container" class="d-flex justify-content-center">
        <div class="card">
            <div class="card-header text-center">
                <h2>
                    Recent Posts
                </h2>
            </div>
            <div id="admin-posts-container">
                {% include 'partials/postsList.twig' %}
            </div>
        </div>
    </div>

{% endblock %}